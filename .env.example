# No browser gRPC-Web URL is needed for the default deployment shape.
# The GUI calls the same origin that served the page, and Envoy Gateway routes
# /controlpanel.v1.SignalStimService/* requests to the gRPC-Web/backend path.
