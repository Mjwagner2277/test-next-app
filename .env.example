# Browser code loads this value from the Next.js /api/connect-config route.
# Envoy then forwards to the Connect protocol server configured in Helm values.
CONNECT_PROXY_URL=http://localhost:8080

# This is public because /api/connect-config returns it to the browser. Leave
# empty unless the token is safe for end users to inspect.
CONNECT_AUTH_TOKEN=
